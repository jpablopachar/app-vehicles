namespace Infrastructure.Authentication;

/// <summary>
/// Clase que contiene las constantes de claims personalizados utilizados en la autenticación.
/// </summary>
public static class CustomClaims
{
    /// <summary>
    /// Constante que representa el claim de permisos del usuario.
    /// </summary>
    internal const string Permissions = "permissions";
}
